<h1><%= @pitch.name %> - who voted?</h1>

<% if logged_in? and current_user.has_finished_voting? -%>

  <% if @pitch.votes.any? -%>
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
      <% @pitch.votes.each do |vote| %>
        <tr>
          <td><h3><%= vote.user.name %></h3></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <% else -%>
  <p>No votes yet.</p>
  <% end -%>

<%- else %>

<p>You have to be logged in and voted on 3 projects before you can see the votes.</p>

<% end -%>

<p><%= link_to "Go back to pitches", root_path %>
